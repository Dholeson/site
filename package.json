{
  "name": "dhole-dev-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "dhole.dev - projects and retro console store",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "test": "node test/checkout.test.js && node test/gen1-parser.test.js",
    "sync:gen1": "node scripts/sync-gen1.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
